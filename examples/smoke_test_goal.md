# Smoke Test Goal

Create a Python file called `solution.py` in the working directory that contains a function `is_prime(n)` which returns `True` if n is a prime number and `False` otherwise. Handle edge cases: n < 2 returns False. Then run the tests in `test_solution.py` to verify correctness.

The test file `test_solution.py` already exists in the working directory.
